/**
 * Cafe Management Application Roles Enum
 * Exactly 2 Roles: ADMIN and STAFF
 */
export const ROLES = Object.freeze({
  ADMIN: 'ADMIN',
  STAFF: 'STAFF',
});

export const ROLE_LIST = Object.values(ROLES);
